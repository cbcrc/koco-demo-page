{
  "name": "koco-demo-page",
  "version": "1.2.0",
  "homepage": "A demo page used by generator-koco",
  "authors": [
    "Frédérik Charest & Maxime Séguin"
  ],
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp -r src/* dist && rm -rf dist/*.js && babel src --out-dir dist --modules umd --presets es2015,stage-0"
  },
  "dependencies": {
    "knockout": "^3.4.0",
    "koco": "^1.0.0",
    "koco-i18next": "^1.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-stage-0": "^6.5.0"
  }
}
